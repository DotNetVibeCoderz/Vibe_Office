# xunit.v3 runs on Microsoft.Testing.Platform. The .NET 10 SDK dropped VSTest support for it, so
# `dotnet test` has to be told which runner to use here rather than per project.
[dotnet.test.runner]
name = "Microsoft.Testing.Platform"
