@inherits LayoutComponentBase
@*
    The signed-in layout for the desktop and mobile hosts. Identical in effect to the web host's
    MainLayout — both exist only to hand Body to the shared shell, and neither owns any chrome.
*@

<AppShell Body="@Body" />
