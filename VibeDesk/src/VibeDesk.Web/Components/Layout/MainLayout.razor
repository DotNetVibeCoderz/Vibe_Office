@inherits LayoutComponentBase
@*
    The signed-in layout. Delegates entirely to the shared shell so the desktop and mobile hosts get
    the same chrome from the same component.
*@
<AppShell Body="@Body" />
